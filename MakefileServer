all: server

server: server.o protocol.o 
	gcc server.o protocol.o -lsqlite3 -o server

server.o: server.c
	gcc -lsqlite3 -c server.c

protocol.o: protocol.c
	gcc -lsqlite3 -c protocol.c

clean:
	rm -rf *.o server
